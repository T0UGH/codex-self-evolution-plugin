{
  "name": "codex-self-evolution",
  "version": "0.7.1",
  "description": "Scheduled self-evolution loop with stable background memory, provider-backed stop review, explicit suggestion states, launchd-style compiler preflight, focused recall, managed skills, and per-profile api_key_env so multiple anthropic-style providers can coexist.",
  "author": {
    "name": "T0UGH",
    "url": "https://github.com/T0UGH"
  },
  "homepage": "https://github.com/T0UGH/codex-self-evolution-plugin",
  "license": "MIT",
  "commands": [
    {
      "name": "session-start",
      "command": "codex-self-evolution session-start --from-stdin"
    },
    {
      "name": "stop-review",
      "command": "codex-self-evolution stop-review --from-stdin"
    },
    {
      "name": "compile-preflight",
      "command": "codex-self-evolution compile-preflight --state-dir \"$CODEX_STATE_DIR\""
    },
    {
      "name": "compile",
      "command": "codex-self-evolution compile --once --state-dir \"$CODEX_STATE_DIR\" --backend agent:opencode"
    },
    {
      "name": "scan",
      "command": "codex-self-evolution scan --backend agent:opencode"
    },
    {
      "name": "status",
      "command": "codex-self-evolution status"
    },
    {
      "name": "recall",
      "command": "csep recall \"$CODEX_RECALL_QUERY\" --cwd \"$CODEX_CWD\" --state-dir \"$CODEX_STATE_DIR\""
    },
    {
      "name": "recall-trigger",
      "command": "codex-self-evolution recall-trigger --query \"$CODEX_RECALL_QUERY\" --cwd \"$CODEX_CWD\" --state-dir \"$CODEX_STATE_DIR\""
    }
  ],
  "hooks": "./hooks.json",
  "scheduler": {
    "recommended": "launchd",
    "scan_command": "codex-self-evolution scan --backend agent:opencode",
    "preflight_command": "codex-self-evolution compile-preflight --state-dir \"$CODEX_STATE_DIR\"",
    "compile_command": "codex-self-evolution compile --once --state-dir \"$CODEX_STATE_DIR\" --backend agent:opencode"
  }
}
