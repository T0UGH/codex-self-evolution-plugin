# CSEP 当前状态记录

记录时间：2026-05-16 19:58 CST

本文记录 `csep` 发布后的项目状态、真实本机运行状态，以及三条主线的下一步优化点。

最新交接更新：2026-05-20 13:07 CST。下面 `1.3.4` / `1.3.3` / `1.3.2` / `1.3.1` / `1.3.0` / `1.2.0` / `1.1.0` 段落保留为历史状态；当前真实状态以最新发布节为准。

## 2026-05-20 1.3.5 发布状态

### 发布和远端

- PyPI 已发布：`csep 1.3.5`
- PyPI 页面：https://pypi.org/project/csep/1.3.5/
- `1.3.5` 包含 wheel 和 sdist：
  - `csep-1.3.5-py3-none-any.whl`
  - `csep-1.3.5.tar.gz`
- 发布前验证：
  - `uv run pytest -q` -> `263 passed`
  - `uv run --extra dev python -m build` 成功
  - `uv run --extra dev twine check dist/csep-1.3.5*` passed
  - `uv run --extra dev twine upload dist/csep-1.3.5*` 成功
- 构建仍会出现既有 warning：
  - `pyproject.toml` license table / license classifier 的 setuptools deprecation warning
  - `MANIFEST.in` 中 `tests/fixtures/*.json` 当前无匹配文件

### 1.3.5 主变更

- `session_reflection` prompt 明确：duplicate-only / no-new-candidate 复盘只写 `receipt.json`，不得写入 `MEMORY.md` 或 `memory/refs/`。
- `validate_receipt()` 新增 `low_value_memory_writes` 检测，低价值反射流水账写入会降级为 `partial`。
- runner 的 trigger reset 逻辑会把 `low_value_memory_writes` 视为 memory scope issue，避免这类写入重置 memory review 计数。

## 2026-05-17 1.3.4 发布状态

### 发布和远端

- PyPI 已发布：`csep 1.3.4`
- PyPI 页面：https://pypi.org/project/csep/1.3.4/
- `1.3.4` 包含 wheel 和 sdist：
  - `csep-1.3.4-py3-none-any.whl`
  - `csep-1.3.4.tar.gz`
- 发布前验证：
  - `csep config validate` -> ok
  - `scripts/smoke-runtime.sh` -> passed
  - `make test PYTHON=/Users/haha/hermes-agent/venv/bin/python3.11` -> `261 passed`
  - `uv run --extra dev python -m build` 成功
  - `uv run --extra dev twine check dist/csep-1.3.4*` passed
  - `uv run --extra dev twine upload dist/csep-1.3.4*` 成功
  - `git diff --check` clean
- 构建仍会出现既有 warning：
  - `pyproject.toml` license table / license classifier 的 setuptools deprecation warning
  - `MANIFEST.in` 中 `tests/fixtures/*.json` 当前无匹配文件

### 1.3.4 主变更

- README / README_en 将 `zero runtime dependencies` 收口为 `zero Python runtime dependencies`，避免误导成不依赖 Codex / uv / app-server。
- `docs/session-recall.md` 明确 `SessionStart` 注入的是短 recall policy/pointer，不是完整 recall 操作手册。
- `docs/session-reflection.md` 和 getting started 补充默认 `danger-full-access` + `approval_policy = "never"` 的执行模型、风险前提和父进程 validation 边界。
- 新增 `scripts/smoke-runtime.sh`，使用临时 state dir 验证：
  - `csep status` 版本矩阵
  - `SessionStart` hook 输出
  - Codex transcript archive + recall
  - Doubao Code sync + recall
- `tests/test_install_script.py` 覆盖 runtime smoke 脚本入口和关键路径。

### 下一步建议

- 后续 release gate 可以固定先跑 `scripts/smoke-runtime.sh`，再跑全量 pytest。
- 仍可继续考虑一条更接近真实 Codex app-server 的半自动 e2e harness，但这不再阻塞当前发布。

## 2026-05-17 1.3.3 发布状态

### 发布和远端

- PyPI 已发布：`csep 1.3.3`
- PyPI 页面：https://pypi.org/project/csep/1.3.3/
- `pip --no-cache-dir index versions csep` 已确认可见版本：`1.3.3, 1.3.2, 1.3.1, 1.3.0, 1.2.0, 1.1.0, 1.0.0`
- `1.3.3` 包含 wheel 和 sdist：
  - `csep-1.3.3-py3-none-any.whl`
  - `csep-1.3.3.tar.gz`
- 发布前验证：
  - `csep config validate` -> ok
  - `make test PYTHON=/Users/haha/hermes-agent/venv/bin/python3.11` -> `260 passed`
  - `uv run --extra dev python -m build` 成功
  - `uv run --extra dev twine check dist/csep-1.3.3*` passed
  - `uv run --extra dev twine upload dist/csep-1.3.3*` 成功
  - `git diff --check` clean
- 本机安装验证：
  - `csep setup --package csep==1.3.3 --skip-marketplace` -> ok
  - `uv tool list` 显示 `csep v1.3.3`
  - `csep --version` -> `csep 1.3.3`
  - `codex-self-evolution --version` -> `codex-self-evolution 1.3.3`
  - `csep status` 中 installed/runtime/source/PyPI 均为 `1.3.3` 且匹配
  - `csep recall sync-claude --state-dir <tmp> --limit-files 1 --format json` -> `completed`
  - 临时 DB 中 `csep recall --recent --global` -> `matched`
- 构建仍会出现既有 warning：
  - `pyproject.toml` license table / license classifier 的 setuptools deprecation warning
  - `MANIFEST.in` 中 `tests/fixtures/*.json` 当前无匹配文件

### 1.3.3 主变更

- `csep recall sync-claude` 可将本机 Doubao Code 历史同步进现有 session recall SQLite/FTS。
- 默认读取 `~/.claude/projects/**/*.jsonl`，并支持 `--root`、`--since-days`、`--all-history`、`--limit-files`、`--format json`、`--state-dir`。
- Claude Code session 写入同一 recall store，但 `sessions.source` 标记为 `claude_code_jsonl`。
- Claude session id 加 `claude:` 前缀，避免与 Codex session id 冲突。
- parser 会索引 Claude user/assistant 文本、`tool_use`、`tool_result`，并跳过 `thinking` 块。
- `csep recall claude` 保持普通搜索语义，不会误触发同步。
- `csep-session-recall` skill 文档已更新，明确支持导入后的 Claude Code 会话。

### 下一步建议

- 真实导入前可先用 `--limit-files` 做小批量 smoke，再决定是否执行 `--all-history`。
- 继续观察大量 Claude Code 历史导入后的 recall 噪音和排序质量；必要时再加 source filter 或 Claude 专属检索权重。

## 2026-05-17 1.3.2 发布状态

### 发布和远端

- PyPI 已发布：`csep 1.3.2`
- PyPI 页面：https://pypi.org/project/csep/1.3.2/
- `pip --no-cache-dir index versions csep` 已确认可见版本：`1.3.2, 1.3.1, 1.3.0, 1.2.0, 1.1.0, 1.0.0`
- `1.3.2` 包含 wheel 和 sdist：
  - `csep-1.3.2-py3-none-any.whl`
  - `csep-1.3.2.tar.gz`
- 发布前验证：
  - `csep config validate` -> ok
  - `make test PYTHON=/Users/haha/hermes-agent/venv/bin/python3.11` -> `257 passed`
  - `uv run --extra dev python -m build` 成功
  - `uv run --extra dev twine check dist/csep-1.3.2*` passed
  - `uv run --extra dev twine upload dist/csep-1.3.2*` 成功
  - `git diff --check` clean
- 构建仍会出现既有 warning：
  - `pyproject.toml` license table / license classifier 的 setuptools deprecation warning
  - `MANIFEST.in` 中 `tests/fixtures/*.json` 当前无匹配文件

### 1.3.2 主变更

- 顶层 `csep --version` 可用，输出 `csep <version>`。
- 顶层 `codex-self-evolution --version` 也可用。
- `csep status` 不再通过 `csep --help` 探测 CSEP 版本，避免 `tools.csep.version` 显示成 `usage: csep [-h]`。
- `tools.csep` 新增版本矩阵：
  - `installed_version`
  - `runtime_version`
  - `source_version`
  - `pypi_latest_version`
  - `runtime_matches_installed`
  - `runtime_matches_source`
  - `runtime_matches_pypi`
  - `source_matches_pypi`

### 下一步建议

- 用一段真实使用时间观察 version diagnostics 的 PyPI 查询是否需要缓存或超时调优。
- `review_memory` 和 `review_skills` 同时触发保留为当前设计，不作为缺陷处理。

## 2026-05-17 1.3.1 发布状态

### 发布和远端

- PyPI 已发布：`csep 1.3.1`
- PyPI 页面：https://pypi.org/project/csep/1.3.1/
- `pip --no-cache-dir index versions csep` 已确认可见版本：`1.3.1, 1.3.0, 1.2.0, 1.1.0, 1.0.0`
- `1.3.1` 包含 wheel 和 sdist：
  - `csep-1.3.1-py3-none-any.whl`
  - `csep-1.3.1.tar.gz`
- 发布前验证：
  - `csep config validate` -> ok
  - `make test PYTHON=/Users/haha/hermes-agent/venv/bin/python3.11` -> `254 passed`
  - `uv run --extra dev python -m build` 成功
  - `uv run --extra dev twine check dist/csep-1.3.1*` passed
  - `uv run --extra dev twine upload dist/csep-1.3.1*` 成功
  - `git diff --check` clean
- 构建仍会出现既有 warning：
  - `pyproject.toml` license table / license classifier 的 setuptools deprecation warning
  - `MANIFEST.in` 中 `tests/fixtures/*.json` 当前无匹配文件

### 1.3.1 主变更

- Stop hook archive 先执行 archive guard，只跳过明确的 reflection child thread。
- `memory_consolidation` payload、child thread registry 命中、或 transcript 中的 `CSEP_REFLECTION_JOB_ID=` / `CSEP_REFLECTION_CHILD=1` 标记都会被判定为 child 并返回 `skipped`。
- `csep session-archive` 对 `skipped` 返回成功退出码，避免后台 child archive 被当成失败。
- 非 child 且缺 `transcript_path` 的 Stop payload 会做高置信 transcript discovery：
  - filename 包含 `session_id`
  - 最近 transcript 的 `session_meta.id` 等于 `session_id`
  - 最近 transcript 中 `session_meta.cwd` 与 payload `cwd` 唯一匹配
- 多候选会记录 `discovery_ambiguous`，不会乱归档，避免污染 recall DB。

### 下一步建议

- 继续观察真实 Stop hook 后续是否还出现新的 `missing transcript_path`；旧 DB 里的历史错误仍会保留。
- 后续仍建议补：
  - 顶层 `csep --version`
  - `csep status` 显示 installed/source/PyPI 版本差异

## 2026-05-17 1.3.0 发布状态

### 发布后本地修复

- Stop hook archive 已补 child guard：明确的 reflection child thread 不再进入 session recall archive，也不再生成 `missing transcript_path` 噪音。
- child 判定信号：
  - `thread_source` / `threadSource` / `source` 为 `memory_consolidation`
  - `session_id` / `thread_id` 命中 `session_reflection/child_threads/` registry
  - 已发现 transcript 中包含 `CSEP_REFLECTION_JOB_ID=` 或 `CSEP_REFLECTION_CHILD=1`
- 缺 `transcript_path` 的非 child Stop payload 会尝试高置信 transcript discovery：
  - filename 包含 `session_id`
  - 最近 transcript 的 `session_meta.id` 等于 `session_id`
  - 最近 transcript 中 `session_meta.cwd` 与 payload `cwd` 唯一匹配
- 多候选时记录 `discovery_ambiguous`，不归档，避免把 child 或错误 session 写进 recall DB。
- 本地安装版已通过 `scripts/install.sh` 刷新，真实 hook 会使用当前修复。
- 验证：
  - targeted pytest -> `38 passed`
  - `make test PYTHON=/Users/haha/hermes-agent/venv/bin/python3.11` -> `254 passed`
  - `git diff --check` clean
  - `csep session-archive --from-hook-payload <memory_consolidation payload>` -> `skipped`

### 发布和远端

- PyPI 已发布：`csep 1.3.0`
- PyPI 页面：https://pypi.org/project/csep/1.3.0/
- `pip index versions csep` 已确认可见版本：`1.3.0, 1.2.0, 1.1.0, 1.0.0`
- `1.3.0` 包含 wheel 和 sdist：
  - `csep-1.3.0-py3-none-any.whl`
  - `csep-1.3.0.tar.gz`
- 发布前验证：
  - `csep config validate` -> ok
  - `make test PYTHON=/Users/haha/hermes-agent/venv/bin/python3.11` -> `248 passed`
  - `uv run --extra dev python -m build` 成功
  - `uv run --extra dev twine check dist/csep-1.3.0*` passed
  - `uv run --extra dev twine upload dist/csep-1.3.0*` 成功
  - `git diff --check` clean
- 构建仍会出现既有 warning：
  - `pyproject.toml` license table / license classifier 的 setuptools deprecation warning
  - `MANIFEST.in` 中 `tests/fixtures/*.json` 当前无匹配文件

### 本机安装状态

- 本机已安装 PyPI 版：`csep 1.3.0`
- `uv tool list` 显示：`csep v1.3.0`
- 已执行：`csep setup --package csep==1.3.0 --skip-marketplace`
- `csep config validate`：ok
- `csep status` 能从 uv tool 安装目录读取 plugin bundle：
  - plugin root: `/Users/haha/.local/share/uv/tools/csep/lib/python3.13/site-packages/codex_self_evolution/plugin_bundle`
  - `SessionStart`: `csep session-start --from-stdin`
  - `Stop`: `csep session-stop --from-stdin`
- `csep status` 已输出新的 `stable_memory` 区块：
  - `bucket_count`: `3`
  - `legacy_user_md_ignored`: true
  - 当前 repo bucket 的 `refs_dir_exists`: true
  - `latest_memory_validation_status`: `skipped_empty`
  - global reflection lock: not locked

### 1.3.0 主变更

- Stable Memory 收敛为单一 `MEMORY.md` + `memory/refs/` 模型
- `SessionStart` 只读取并注入当前 repo bucket 的 `MEMORY.md`，不再读取 `USER.md`
- `memory/refs/` 会作为按需引用区创建，但不会在 `SessionStart` 自动展开
- reflection prompt 只暴露 `MEMORY.md` 和 `memory/refs/` 作为 memory 目标
- parent validation 只接受 `memory/MEMORY.md` 和 `memory/refs/**/*.md`，不再接受 `USER.md`
- `csep status` 新增 `stable_memory` 诊断区块，提示 legacy `USER.md` ignored、refs 数量和最新 memory validation 状态

### 下一步建议

- 新开 Codex session 后确认 harness 载入 PyPI `1.3.0` 的 plugin bundle 和 skill。
- 继续观察真实 reflection 对 `memory/refs/` 的整理质量。
- 后续仍建议补：
  - 顶层 `csep --version`
  - `csep status` 显示 installed/source/PyPI 版本差异

## 2026-05-17 1.2.0 历史交接状态

### 发布和远端

- PyPI 已发布：`csep 1.2.0`
- PyPI 页面：https://pypi.org/project/csep/1.2.0/
- PyPI JSON 已确认 `1.2.0`，包含 wheel 和 sdist：
  - `csep-1.2.0-py3-none-any.whl`
  - `csep-1.2.0.tar.gz`
- 最新提交：`301371e release: publish csep 1.2.0`
- 远端状态：`main` 已推送到 `origin/main`
- 发布前验证：
  - `make test PYTHON=/Users/haha/hermes-agent/venv/bin/python3.11` -> `244 passed`
  - `uv run --extra dev python -m build` 成功
  - `uv run --extra dev twine check dist/csep-1.2.0*` passed
  - `git diff --check` clean
- 构建仍会出现既有 warning：
  - `pyproject.toml` license table / license classifier 的 setuptools deprecation warning
  - `MANIFEST.in` 中 `tests/fixtures/*.json` 当前无匹配文件

### 本机安装状态

- 本机已安装 PyPI 版：`csep 1.2.0`
- `csep` 路径：`/Users/haha/.local/bin/csep`
- `uv tool list` 显示：`csep v1.2.0`
- 已执行：`csep setup --package csep==1.2.0`
- `csep config validate`：ok
- `csep status` 能从 uv tool 安装目录读取 plugin bundle：
  - plugin root: `/Users/haha/.local/share/uv/tools/csep/lib/python3.13/site-packages/codex_self_evolution/plugin_bundle`
  - `SessionStart`: `csep session-start --from-stdin`
  - `Stop`: `csep session-stop --from-stdin`
- Codex 配置已启用：
  - `[features] plugins = true`
  - `[features] hooks = true`
  - `[features] plugin_hooks = true`
  - `[plugins."codex-self-evolution@codex-self-evolution"] enabled = true`
- Codex marketplace 已添加：
  - source: `https://github.com/T0UGH/codex-self-evolution-plugin.git`
  - marketplace temp root: `/Users/haha/.codex/.tmp/marketplaces/codex-self-evolution`

### Plugin skill 安装验证

- CSEP plugin manifest 已声明：`"skills": "./skills/"`
- harness 可见的 skill 文件路径：
  - `/Users/haha/.codex/.tmp/marketplaces/codex-self-evolution/plugins/codex-self-evolution/skills/csep-session-recall/SKILL.md`
- PyPI 安装目录里的 plugin bundle 也包含：
  - `/Users/haha/.local/share/uv/tools/csep/lib/python3.13/site-packages/codex_self_evolution/plugin_bundle/skills/csep-session-recall/SKILL.md`
- 当前正在运行的 Codex session 不会热加载新 plugin skill；新开 session 后 harness 才会看到 `csep-session-recall`。

### 安装版 recall 试用结果

安装版 `csep 1.2.0` 已用真实历史 case 试过：

- `csep recall "Recall|grep-like|plugin skill|evidence window|scope fallback"`：matched
- `csep recall "summary需要llm 不要llm Hermes"`：matched，普通空格 query 的 OR fallback 生效
- `csep recall "MEMORY.md|refs|二级引用"`：matched
- `csep recall --recent`：preview 已优先显示真实 user 任务，不再被 AGENTS / stable background 注入污染
- `csep recall "Recall|grep-like|plugin skill" --format json`：结构化 JSON 输出正常
- `csep recall "exec_command|apply_patch"`：现在能命中刚才真实 session 中关于旧 DB `tool_name` 缺失的讨论；这不是旧 tool_name 索引突然恢复，而是命中了 transcript 文本本身

当前 recall DB 状态：

- DB path: `/Users/haha/.codex-self-evolution/session_recall/state.db`
- sessions: `313`
- messages: `5612`
- `~/.codex/sessions` 下约 `326` 个 `.jsonl`
- ingest error total: `17`
- latest ingest error: `missing transcript_path`

### SessionStart 验证

手动模拟：

```bash
printf '{"cwd":"/Users/haha/workspace/codex-self-evolution-plugin","session_id":"manual-install-smoke"}' | csep session-start --from-stdin
```

输出里已经不再注入完整 recall contract，只保留短 pointer：

```text
For historical session search, use the `csep-session-recall` plugin skill.
Treat `csep recall` like `rg` over past sessions: search short concrete needles, not long natural-language questions.
```

### 下一 session 建议

- 新开 Codex session 后，先确认 harness 是否加载到 `csep-session-recall` skill。
- 继续使用 `csep recall` 时按 skill 里的 grep-like 用法：搜索短 needle，不搜长自然语言问题。
- 若要继续做 CSEP 代码变更，测试仍使用：
  - `make test PYTHON=/Users/haha/hermes-agent/venv/bin/python3.11`
- 后续仍建议补：
  - 顶层 `csep --version`
  - `csep status` 显示 installed/source/PyPI 版本差异
  - Stop hook `transcript_path` discovery fallback

## 发布与仓库状态

- 当前分支：`main`
- 最新提交：`537418c release: publish csep 1.1.0`
- 远端状态：`main` 已同步 `origin/main`
- PyPI：`csep 1.1.0` 已发布并可通过 PyPI JSON 直查
- 源码版本：`codex_self_evolution.__version__ == "1.1.0"`
- 本机 uv tool：仍是 `csep 1.0.0`

本机 uv tool 尚未刷新，因此直接运行 `csep` 时仍看不到 `recall bootstrap` 参数。用当前源码运行时，`csep recall bootstrap` 和新的 status 字段可用。这个差异会影响真实 hook 行为，是下一轮优先修复点。

## 验证状态

- `csep config validate`：ok
- `make test`：236 passed
- `git status`：记录前为 clean

已知构建警告仍存在：

- `pyproject.toml` 的 license table / license classifier 会触发 setuptools 去年性告警
- `MANIFEST.in` 仍包含 `tests/fixtures/*.json`，当前仓库无匹配文件

## Runtime 状态

源码路径下运行 `csep status` 的关键信号：

- plugin hook bundle 存在，`SessionStart` / `Stop` 均已声明
- `SessionStart` 命令：`csep session-start --from-stdin`
- `Stop` 命令：`csep session-stop --from-stdin`
- recall DB：`313` sessions / `5348` messages
- `~/.codex/sessions`：`326` 个 `.jsonl`
- `backfill_recommended`：false
- latest ingest run：1 file processed, 1 updated, 0 errors
- ingest error total：14
- latest ingest error：`missing transcript_path`
- latest reflection job：`succeeded`
- latest reflection validation：`succeeded`
- reflection global lock：not locked
- `.env.provider`：不存在

## 三条主线状态

### Stable Memory

现状：

- `SessionStart` 只读取当前 repo bucket 的 `MEMORY.md`
- 空文件会注入 `_No entries yet._`
- `memory/refs/` 作为按需引用区存在，不会在 `SessionStart` 自动展开
- 旧 `USER.md` 如果存在会保留在磁盘上，但 runtime 忽略并在 `csep status` 中提示

已完成：

- 取消 `USER.md` / `MEMORY.md` 双文件注入模型，收敛到唯一有效的 `MEMORY.md`
- `SessionStart` 全文注入 `MEMORY.md`，不截断、不过滤、不读取 `refs/`
- reflection prompt 只暴露 `MEMORY.md` 和 `memory/refs/` 作为 memory 目标
- parent validation 只接受 `memory/MEMORY.md` 和 `memory/refs/**/*.md`，不再接受 `USER.md`
- `csep status` 增加 `stable_memory` 区块，提示 memory 文件、大小、legacy `USER.md` ignored、refs 数量和最新 memory validation 状态

仍需打磨：

- memory linter / secret warning 仍只是后续可观测性增强，不作为 hard block
- review agent 对 `refs/` 的整理质量仍需要通过真实 reflection 运行继续观察

### Session Recall

现状：

- Stop hook 和 backfill 都写入 SQLite/FTS
- `csep recall bootstrap` 已作为正式入口发布
- backfill 已按 transcript mtime 新到旧导入
- status 能检测历史 transcript 数量并给出 bootstrap 建议

问题：

- 本机已安装 tool 仍是 `1.0.0`，真实 hook 暂时还跑旧代码
- `csep recall` 查不到时，还不会基于 DB / `~/.codex/sessions` 状态给自救提示
- Stop hook 缺 `transcript_path` 的本地修复已完成：先跳过 reflection child，再对真实缺路径 Stop payload 做高置信 discovery
- 搜索排序仍主要依赖 FTS5 + 简单 role 权重

本轮讨论后的方向：

- Recall 是本地历史证据检索，不是问答工具，也不使用 LLM summary
- 使用心智改为 `rg over past sessions`：搜索短 needle，不搜索长自然语言问题
- Recall 使用说明作为 CSEP 插件自带 skill 暴露给 Codex harness，不再通过 `SessionStart` 注入完整 contract
- CLI 也配合 grep-like 心智：支持 `|` OR alias，普通多 token query 可 strict -> OR fallback
- 默认输出 session-level extractive evidence window，不做重 UI、不做 embedding
- 每个 session 默认展示 2 个 evidence window，可用 `--windows-per-session` 调整
- 默认 evidence window 优先 `user` / `assistant` / `tool`，`developer` / `system` 可索引但不优先展示
- `no_match` 保持简单，不输出统计、不输出 bootstrap / docs / MEMORY 模板建议
- CLI 不自动 repo -> global fallback；需要跨 repo 时由 agent 显式加 `--global`
- 实施计划已落到 `docs/implementation-plans/2026-05-16-session-recall-implementation-plan.md`
- 实现已完成：plugin skill、SessionStart 短提示、grep-like query、session evidence windows、developer/system 降噪、recent preview 修正
- 真实搜索质量验证已记录到 `docs/recall-search-quality-2026-05-16.md`

建议：

- 增加 `csep --version`，并让 `csep status` 显示 installed/source/PyPI 版本差异
- 继续观察 Stop hook discovery 的真实命中率和 ambiguous 记录
- 新增插件 skill：`skills/csep-session-recall/SKILL.md`
- 插件 manifest 增加 `"skills": "./skills/"`，并保持 source bundle / marketplace bundle 一致
- `csep recall` 支持 `|` OR alias、strict -> OR fallback、`--all-terms`
- 搜索排序加入 recency、repo/worktree 精确度、同 session 多 hit、role 权重和工具名权重
- 参考 Hermes 后续补 CJK / trigram / LIKE fallback，但不引入 LLM

### Reflection Skills

现状：

- trigger policy 根据 stop 次数、可读文本、工具调用和高信号关键词决定是否排队
- reflection worker fork child Codex
- child 只允许写 memory 和 `csep-reflect-*` skill
- parent 校验 receipt、路径、hash、skill frontmatter 和必需章节

问题：

- `build_reflection_prompt()` 已能遵守 `review_memory` / `review_skills`
- trigger 一旦 queued，`review_memory` 和 `review_skills` 都为 true，没有区分 memory-only / skill-only
- active skill 只有结构校验，没有重复检测、替代关系或实际加载验证
- 默认 `one_shot_active` 可以保持简单，但需要更好的边界约束，避免生成不该进入 skill 的内容

本轮讨论后的方向：

- Reflection Skills 只承接反复出现、可复用、步骤明确的工作流
- 一次性任务结论、具体状态、项目事实、用户偏好、未跑通临时方案不进入 skill
- reflection 必须 scope-aware：memory 触发只审 memory，skill 触发才审 skill；两者同时触发时才一起审
- prompt、trigger、receipt validation 都要遵守 `review_memory` / `review_skills`
- 短期不优先引入复杂发布门槛，skill 机制尽量保持简单
- 一个合格的 `csep-reflect-*` `SKILL.md` 仍可直接 active
- `one_shot_active` 可以继续作为简单默认，但必须配合 scope-aware prompt、严格结构校验和边界校验
- 发布前做重复和冲突检测，避免同类 workflow 反复生成多个 skill
- `evidence` / `draft` / lifecycle metadata 作为后续可选增强，不作为这一轮优先项

建议：

- trigger 按触发原因生成 scope-aware decision
- 保留简单 active skill 模型，先补重复/冲突检测和 activation/load 验证
- 加强 receipt validation：当 `review_skills=false` 时不允许新增或修改 skill
- 将 evidence-first / draft lifecycle / `.csep-skill.json` metadata 留作后续可选增强

## 下一轮建议优先级

P0：

- `csep --version` + status 版本一致性检查
- CSEP plugin 内置 `csep-session-recall` skill
- `csep recall` grep-like query：`|` OR alias、strict -> OR fallback、`--all-terms`
- recall evidence window 降噪：优先 user/assistant/tool，recent preview 优先第一条 user message
- reflection scope-aware trigger

P1：

- `csep setup --backfill-recall --since-days 30`
- recall session-level ranking 和 `--windows-per-session`
- skill duplicate/conflict detection

P2：

- memory linter / secret warning 提示
- Recall CJK / trigram / LIKE fallback
- reflection 质量评估与可选 skill lifecycle / 发布门槛
- PyPI / installed tool 刷新后的端到端安装验证脚本
